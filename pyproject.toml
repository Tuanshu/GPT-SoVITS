[project]
name = "gpt-sovits"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
# not really using now, just for type hint
dependencies = ["fastapi", "pydantic"]
